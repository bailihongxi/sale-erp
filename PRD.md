# 家电批发进销存 ERP — 产品需求文档（PRD）v1.1

> 本文档沉淀已确认的产品目标、架构决策与 v1 范围，作为后续开发与验收的统一依据。
> 适用范围：家电（电器）批发 + 零售商户的进销存（采购-销售-库存）管理。
> 版本：v1.1（本地优先、纯前端、无登录）。状态：已开发完成并验证（2026-08-17，GitHub Pages 已上线）。

---

## 1. 产品目标和使用场景

**产品目标**
- 给家电批发/零售商户提供一套**零账号、零后端、零部署成本**的进销存管理工具。
- 业务数据**完全保存在本机浏览器本地**，刷新 / 关闭 / 重启电脑都不丢失，保障商户对数据的掌控。
- 可一键部署到 GitHub Pages 联网访问，手机浏览器打开即用（以查看数据为主）。

**核心使用场景**
| 角色 | 场景 | 关键操作 |
|---|---|---|
| 收银员 | 门店零售/批发开单 | 选商品 → 填数量/单价 → 收款（含欠款）→ 出库 |
| 仓管 | 进货入库、库存盘点 | 新建进货单入库、库存调整、低库存预警 |
| 老板/店长 | 看经营状况 | 工作台看板、报表分析、应收应付 |
| 任意人（手机） | 随时查数据 | 手机看板/商品/库存/我的，必要时导入备份 |

**硬约束（已确认）**
- 不需要登录，无账号体系。
- 数据只在**当前 Mac 的浏览器本地**；Mac 与手机**各自独立、互不同步**。
- 跨设备搬数据靠「导出/导入备份（JSON）」手动完成，不接云端自动同步。

---

## 2. 整体页面框架和导航结构

**桌面端框架**
- 左侧常驻**侧边栏**（模块导航）+ 顶部**顶栏**（当前页标题、店铺名、快捷备份）+ 右侧**主内容区**（按模块渲染）。
- 采用**哈希路由**（`#dashboard` / `#products` …），无页面刷新切换。

**导航结构（10 个模块，分两组）**
- 经营：工作台 · 商品管理 · 销售开单 · 销售管理 · 采购管理 · 库存管理 · 报表分析 · 财务管理
- 系统：系统设置 · 数据管理

**手机端框架（v1 以查看为主）**
- 顶部栏保留；侧边栏隐藏，改为**底部标签栏**：工作台 / 商品 / 库存 / 我的。
- 「我的」以底部弹出菜单（Sheet）列出全部功能模块，进入开单等操作类页面。

**视觉基调**：现代简约·蓝色商务（主色 `#2D5BE3` + 暖橙强调 `#FF7A33`），克制轻投影，响应式（桌面→平板→手机）。

---

## 3. 首页（工作台）的内容结构

工作台是经营数据聚合页，结构如下：
1. **KPI 卡片（4 张）**：今日营收、今日订单、库存预警数、应收欠款。
2. **近 7 天销售趋势图**：简易柱状图（来自真实销售数据）。
3. **库存预警列表**：库存 ≤ 阈值的商品（名称/品牌/库存数），红标提示。
4. **最近销售**：最近 5 笔销售单（单号/客户/金额/状态）。
5. **热销商品 TOP5**：按销量排序。

所有数字均实时来自本地数据，开单/采购/调整后刷新即更新。

---

## 4. 每个模块的用途

| 模块 | 用途 |
|---|---|
| 工作台 | 经营总览，一屏掌握营收、订单、预警、应收与热销。 |
| 商品管理 | 维护商品档案，是开单/库存/报表的数据基础。 |
| 销售开单 | 最高频操作：现场收银与批发开单，生成销售单并出库。 |
| 销售管理 | 查看历史销售单、详情，对欠款单收款冲抵。 |
| 采购管理 | 建立进货单、供应商入库，增加库存，记录应付。 |
| 库存管理 | 实时库存、低库存预警、出入库流水、库存调整。 |
| 报表分析 | 销售额/估算毛利/应收应付、趋势与排行，辅助决策。 |
| 财务管理 | 应收（客户欠款）/应付（供应商未付）与收付款流水。 |
| 系统设置 | 店铺名称、默认低库存阈值、清空重置数据。 |
| 数据管理 | 导出/导入备份、查看存储信息，保障数据不丢失。 |

---

## 5. 每个模块第一版最必要的数据和操作

**工作台**
- 数据：今日营收、今日订单数、库存预警数、应收总额、近 7 天每日销售额、预警商品、最近销售、热销 TOP5。
- 操作：查看（无写操作）；各区块可点击跳转到对应模块。

**商品管理**（`products`）
- 数据字段：名称、品牌、型号、类型、分类、单位、批发价、零售价、库存、低库存阈值。
- 操作：新增、编辑、删除、按名称/品牌/型号/类型搜索、按分类筛选。

**销售开单**（`sales` + `stockLogs`）
- 必要数据：客户（可散客）、商品（含单价/单位/库存）、数量、优惠、实收、收款方式（现金/微信/支付宝/银行/欠款）。
- 操作：选商品加入购物车 → 改数量/单价 → 填优惠与实收 → 确认结算。结算后：扣减库存、生成销售单、记出库流水、欠款入应收。

**销售管理**（`sales`）
- 数据：单号、日期、客户、金额、已收、状态（已结清/部分收/欠款）。
- 操作：列表查看、详情、对欠款单「收款」（按时间顺序冲抵未结清单据）。

**采购管理**（`purchases` + `stockLogs`）
- 数据：供应商、商品行（数量/单价）、金额、已付、状态。
- 操作：新建进货单 → 入库并增加库存、记入库流水、未付入应付；查看详情。

**库存管理**（`products` + `stockLogs`）
- 数据：实时库存、低库存阈值、出入库流水（日期/类型/商品/数量/备注）。
- 操作：查看库存与流水、库存调整（正/负数量 + 备注，联动库存与流水）。

**报表分析**（聚合）
- 数据：累计销售额、估算毛利（销量×最近采购单价）、应收、应付、近 7 天趋势、商品销量 TOP、客户销售排行、销售笔数、已收、进货总额。
- 操作：查看（v1 不含下钻/导出）。

**财务管理**（`finance` + 派生的应收应付）
- 数据：客户应收欠款、供应商应付未付、收付款流水。
- 操作：对客户「收款」、对供应商「付款」（记流水、冲抵未结清）。

**系统设置**（`settings`）
- 数据：店铺名称、默认低库存阈值。
- 操作：保存设置、清空并重置数据（二次确认）。

**数据管理**
- 数据：各集合条数、店铺名。
- 操作：**导出备份**（下载 JSON）、**导入备份**（选择文件恢复）、查看存储信息。

---

## 6. 首页和每个模块之间的关系

- **首页是聚合层**：其全部数字来自各业务模块的真实数据——营收/订单来自「销售开单/销售管理」，库存预警来自「商品/库存管理」，应收来自「销售+财务」，趋势来自「销售」。
- **数据闭环**：开单（销售开单）→ 出库（库存管理）→ 营收（工作台/报表）→ 欠款（财务/销售管理收款）。采购（采购管理）→ 入库（库存管理）→ 应付（财务）。
- **跳转关系**：首页 KPI/列表项可进入对应模块查看明细；各模块写操作后返回首页数字即时更新（同源 localStorage）。
- **模块间耦合点**：商品档案是所有模块的基础字典；销售单/采购单是库存与财务的源头；库存预警同时被首页、商品管理、库存管理三处引用。

---

## 7. 本地数据文件、备份和恢复要求

**存储机制**
- 纯前端，无后端；数据存放于浏览器 **localStorage**，键名 `sale_erp_v1_state`，内容为单个 JSON。
- 数据结构（集合）：`settings`、`products`、`customers`、`suppliers`、`sales`、`purchases`、`stockLogs`、`finance`。

**持久化要求**
- 刷新网页、关闭标签页、重启电脑后数据**必须保留**（localStorage 同源持久）。
- 数据仅存在于**当前设备当前浏览器**；清除浏览器缓存/换浏览器/换设备会导致数据不可见——因此必须定期备份。

**备份与恢复**
- **导出备份**：将数据序列化为 JSON 文件下载（文件名含日期）。
- **导入备份**：选择 JSON 文件，解析后整体覆盖本地数据并立即生效。
- **跨设备迁移**：Mac 导出 → 目标设备（手机/另一台电脑）导入，实现数据搬运（非实时同步）。
- **清空重置**：保留结构、清空业务数据并恢复初始示例数据（不可撤销，需二次确认）。
- **数据位置隔离（origin）**：同一套代码，以 `双击 index.html`（file://）、`http://localhost:8080`、GitHub Pages 三种方式打开，是**三个互相独立的存储位置**（浏览器按 origin 隔离），数据互不相通。顶栏「📍 数据位置」徽标实时显示当前来源，file:// 方式额外高亮提示。换方式打开 ≠ 同步，迁移请用导出/导入。
- **滚动快照（快速回滚）**：提供「数据管理 → 创建快照」，按环形保留最近 3 份时点快照；异常后可「恢复快照」回到任一历史时点（不影响后续备份文件）。

---

## 8. 第一版必须完成的功能

- [x] 10 个模块全部**真实可用**（非占位/换名列表），覆盖上述第 5 节的数据与操作。
- [x] 商品档案含 **品牌 / 型号 / 类型** 字段及搜索筛选。
- [x] 销售开单：选商品→购物车→多数量多单价→优惠→收款（含欠款/多方式）→出库生成单据。
- [x] 采购入库：新建进货单→入库增库存→应付。
- [x] 库存预警 + 出入库流水 + 库存调整。
- [x] 报表聚合（营收/毛利估算/应收应付/趋势/排行）。
- [x] 财务应收应付与收付款冲抵。
- [x] 数据导出/导入备份。
- [x] 响应式 + 手机端底部导航（以查看为主）。
- [x] GitHub Pages 部署文件（README、.nojekyll、Actions 工作流）。
- [x] **P0 缺陷修复**：账实一致（开单/采购/库存调整联动、收款按时间顺序冲抵）、手机 Sheet 点击自动关闭、写入失败常驻横幅等 8 项阻塞性缺陷（Sprint 1）。
- [x] **客户管理 / 供应商管理**：基础档案 + 往来账款；停用单位禁止被硬删除（归档保护），历史单据保留。
- [x] **购物车改单价 / 档位、采购付款、列表筛选**：开单灵活性、采购收付款、各列表按状态/时间筛选（Sprint 2）。
- [x] **空白账本 / 示例数据双模式**：`DB.reset('blank'|'demo')`，新用户从空白起步或从示例学习（Sprint 3）。
- [x] **投产护栏**：数据位置徽标 + 首屏「只用一种方式打开」引导 + 超 7 天未导出提醒黄条 + 存储用量进度条 + 空态引导（Sprint 3）。
- [x] **手机端体验**：底部标签栏 + Sheet 菜单、安全区适配（viewport-fit=cover）、宽表格横向滚动不撑破布局（Sprint 4）。

---

## 9. 第一版暂时不做的功能

- 打印小票 / 销售单 / 进货单（纸质单据排版）。
- 手机摄像头扫码（商品条码/扫码枪录入）。
- 深色模式。
- 多店铺、多角色/权限（当前无登录，单用户）。
- 报表深度下钻、导出 Excel / PDF。
- 云端自动同步（与「本地优先、互不同步」原则冲突，已确认不做）。
- 客户/供应商完整 CRM（当前仅基础档案与往来账款）。

---

## 10. 可以实验检查的验收标准

**A. 加载与框架**
1. 双击 `index.html` 或访问本地服务，页面无 JS 报错，默认进入工作台。
2. 侧边栏显示 10 个导航项；手机视口（≤768px）出现底部标签栏（工作台/商品/库存/我的）。
3. 点击各导航项，主内容区切换且顶栏标题随之变化（哈希路由生效）。

**B. 商品管理**
4. 商品表格包含「品牌 / 型号 / 类型」三列，且有 ≥1 行示例数据。
5. 新增商品（填名称/品牌/型号/类型等）后，列表出现该行；编辑、删除生效。

**C. 销售开单联动**
6. 点击商品卡 → 购物车出现该商品；加减数量、改单价实时更新小计与合计。
7. 确认结算后：生成销售单；对应商品库存减少；工作台「今日营收/订单」增加；欠款单在「销售管理/财务」可见。
8. 对欠款单「收款」后，其状态变为已结清/部分收，财务应收相应减少。

**D. 采购与库存**
9. 新建进货单并入库后，对应商品库存增加，库存管理出现入库流水。
10. 库存调整（正/负）后库存数值与出入库流水同步更新。

**E. 报表与财务**
11. 报表显示的销售额、应收、应付与开单/采购操作结果一致。
12. 财务应收列表金额 = 各客户销售单（总额−已收）之和；收款后实时变化。

**F. 持久化与备份**
13. 完成开单/新增商品后**刷新页面**，数据仍在（不丢失）。
14. 导出备份生成 JSON 文件；导入该文件后数据被完整恢复。
15. 清除浏览器数据前未备份 → 数据丢失（验证备份必要性）；导入备份可恢复。

**G. 自动化回归（已实现）**
- 数据层：node + localStorage shim 验证 CRUD / 聚合 / 库存联动 / 收款冲抵 / 导出导入。
- 界面层：jsdom 加载 `index.html` 全量渲染，覆盖 10 模块视图、品牌/型号/类型列、开单点击联动、弹窗等 **318 项断言全部通过**。

**H. 投产护栏与移动端（Sprint 3–4 新增）**
16. 顶栏「📍 数据位置」徽标随打开方式变化；file:// 方式高亮警告（非同步提示）。
17. 空白账本模式首屏给出引导；「恢复示例数据」可回到演示态；超 7 天未导出出现提醒黄条。
18. 手机端点「我的」弹出菜单 → 点任意模块后遮罩自动关闭（BUG-06 不再复发）。
19. 手机端（≤768px）宽表格可横向滚动、页面不横向溢出；KPI / 开单页单列堆叠、安全区不被遮挡。
20. 自动化断言总数 **318 项全部通过**（含上述 P0 回归、移动端、护栏验收点）。

---

*文档维护：需求或范围变更时，同步更新本文档对应章节，并标注版本与日期。*
