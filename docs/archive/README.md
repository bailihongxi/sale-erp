# 归档目录

此目录仅存放**历史材料**，不是运行代码，请勿修改、勿据此开发。

| 子目录 | 说明 |
|---|---|
| `design-prototype-v0/` | 早期视觉原型（静态页面 + 设计规范），已被根目录的正式实现取代 |

## 正式运行代码在哪里

```
index.html          应用入口（唯一入口）
assets/style.css    设计系统与响应式
assets/db.js        数据层（localStorage 持久化）
assets/app.js       应用层（路由 + 全部模块视图）
```

改功能请只改上面这四个文件；`docs/archive/` 下的同名文件是旧快照，改了不会生效。
